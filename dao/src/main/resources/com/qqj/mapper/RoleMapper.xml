<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qqj.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.qqj.entity.Role">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="remark" property="remark" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>
    <sql id="Base_Column_List">
         id, name, remark, status, create_time, update_time
    </sql>

    <delete id="deleteByPrimaryKeys" parameterType="java.lang.Long">
        DELETE FROM role
        WHERE id in
        <foreach item="item" index="index" collection="array" open="("
                 separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <select id="getByAdminId" resultType="com.qqj.entity.Role">
        select
            r.id, name, remark
        from role r, admin_role ur
        where r.`status` = 0 AND r.id = ur.role_id AND ur.admin_id = #{adminId,jdbcType=BIGINT}
    </select>

    <select id="getAllByMap" resultType="com.qqj.entity.Role">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        role
        WHERE 1=1
        <if test="id != null">
            AND id = #{id,jdbcType=BIGINT}
        </if>
        <if test="name != null and name.trim() != ''">
            and name like concat('%', #{name,jdbcType=VARCHAR}, '%')
        </if>
        <if test="remark != null">
            AND remark = #{remark,jdbcType=VARCHAR}
        </if>
        <if test="status != null">
            AND status = #{status,jdbcType=TINYINT}
        </if>
        <if test="createTime != null">
            AND create_time = #{createTime,jdbcType=TIMESTAMP}
        </if>
        <if test="updateTime != null">
            AND update_time = #{updateTime,jdbcType=TIMESTAMP}
        </if>
        ORDER BY id DESC
    </select>

</mapper>

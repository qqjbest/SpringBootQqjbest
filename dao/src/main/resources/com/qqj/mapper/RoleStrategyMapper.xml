<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qqj.mapper.RoleStrategyMapper">
    <resultMap id="BaseResultMap" type="com.qqj.entity.RoleStrategy">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="role_id" property="roleId" jdbcType="BIGINT"/>
        <id column="strategy_id" property="strategyId" jdbcType="BIGINT"/>
    </resultMap>

    <delete id="deleteByRoleIds" parameterType="java.lang.Long">
        delete from role_strategy
        WHERE role_id in
        <foreach item="item" index="index" collection="array" open="("
                 separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <delete id="deleteByStrategyIds" parameterType="java.lang.Long">
        delete from role_strategy
        WHERE strategy_id in
        <foreach item="item" index="index" collection="array" open="("
                 separator="," close=")">
            #{item}
        </foreach>
    </delete>
    <delete id="deleteByRoleId" parameterType="java.lang.Long">
        delete from role_strategy
        WHERE role_id = #{roleId,jdbcType=BIGINT}
    </delete>

    <insert id="addTrainRecordBatch" parameterType="java.util.List">
        insert into role_strategy (role_id, strategy_id)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.roleId,jdbcType=BIGINT}, #{item.strategyId,jdbcType=BIGINT})
        </foreach>
    </insert>

</mapper>

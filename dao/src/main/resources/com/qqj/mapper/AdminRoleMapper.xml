<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qqj.mapper.AdminRoleMapper">
    <resultMap id="BaseResultMap" type="com.qqj.entity.AdminRole">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="admin_id" property="adminId" jdbcType="BIGINT"/>
        <id column="role_id" property="roleId" jdbcType="BIGINT"/>
    </resultMap>

    <insert id="addTrainRecordBatch" parameterType="java.util.List">
        insert into admin_role (admin_id, role_id)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.adminId,jdbcType=BIGINT}, #{item.roleId,jdbcType=BIGINT})
        </foreach>
    </insert>

    <delete id="deleteByAdminId" parameterType="java.lang.Long">
        delete from admin_role
        where admin_id = #{adminId,jdbcType=BIGINT}
    </delete>

    <delete id="deleteByRoleIds" parameterType="java.lang.Long">
        delete from admin_role
        WHERE role_id in
        <foreach item="item" index="index" collection="array" open="("
                 separator="," close=")">
            #{item}
        </foreach>
    </delete>
</mapper>
